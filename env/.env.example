# Environment Variables Template
# Copy to env/.env.local (dev), env/.env.alpha, env/.env.release, or env/.env.docker
# Note: root .env is not used in this project.

# Database (required)
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/ivibefinance
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ivibefinance

# Stripe (required for payments)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# App base URL (required)
BASE_URL=http://localhost:3000

# Auth.js secret (required)
# Generate: openssl rand -base64 32
AUTH_SECRET=your-auth-secret

# OpenRouter AI (required for agents)
OPENROUTER_API_KEY=sk-or-v1-xxx

# Google OAuth (required for sign-in)
OAUTH_GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
OAUTH_GOOGLE_CLIENT_SECRET=xxx

# Email (required for magic link / notifications)
EMAIL_SERVER=smtps://user:pass@smtp.gmail.com:465
EMAIL_FROM="iVibeFinance <no-reply@ivibefinance.com>"
